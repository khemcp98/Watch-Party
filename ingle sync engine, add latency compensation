warning: in the working copy of 'client/src/components/Room.jsx', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'server/index.js', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/client/src/components/Room.jsx b/client/src/components/Room.jsx[m
[1mindex fdc0195..0855b3e 100644[m
[1m--- a/client/src/components/Room.jsx[m
[1m+++ b/client/src/components/Room.jsx[m
[36m@@ -1,6 +1,6 @@[m
 import { useEffect, useState } from 'react';[m
 import { socket, connectSocket } from '../lib/socket';[m
[31m-import VideoPlayer from './VideoPlayer';[m
[32m+[m[32mimport UnifiedPlayer from './UnifiedPlayer';[m
 import Chat from './Chat';[m
 import CallPanel from './CallPanel';[m
 import { detectVideoType } from '../lib/videoUrl';[m
[36m@@ -103,7 +103,7 @@[m [mexport default function Room({ roomId, name, onLeave }) {[m
 [m
       <div className="room-body">[m
         <main className="room-main">[m
[31m-          <VideoPlayer[m
[32m+[m[32m          <UnifiedPlayer[m
             roomId={roomId}[m
             videoUrl={videoState.videoUrl}[m
             videoType={videoState.videoType}[m
[1mdiff --git a/client/src/components/VideoPlayer.jsx b/client/src/components/VideoPlayer.jsx[m
[1mdeleted file mode 100644[m
[1mindex f4a1523..0000000[m
[1m--- a/client/src/components/VideoPlayer.jsx[m
[1m+++ /dev/null[m
[36m@@ -1,185 +0,0 @@[m
[31m-import { useEffect, useRef, useState, useCallback } from 'react';[m
[31m-import { parseVideoUrl } from '../lib/videoUrl';[m
[31m-import { useVolumeBoost } from '../lib/useVolumeBoost';[m
[31m-import { socket } from '../lib/socket';[m
[31m-import YouTubePlayer from './YouTubePlayer';[m
[31m-[m
[31m-const SYNC_TOLERANCE = 0.75; // seconds of drift allowed before force-resync[m
[31m-const RESYNC_INTERVAL = 5000;[m
[31m-[m
[31m-export default function VideoPlayer({ roomId, videoUrl, videoType, initialTime, initialPlaying }) {[m
[31m-  const videoRef = useRef(null);[m
[31m-  const [useIframeFallback, setUseIframeFallback] = useState(false);[m
[31m-  const [loadError, setLoadError] = useState(null);[m
[31m-  const suppressEmit = useRef(false);[m
[31m-[m
[31m-  const {[m
[31m-    gain,[m
[31m-    setGain,[m
[31m-    gainPercent,[m
[31m-    muted,[m
[31m-    toggleMute,[m
[31m-    ensureGraph,[m
[31m-    maxGain,[m
[31m-  } = useVolumeBoost(videoRef, { maxGain: 4 });[m
[31m-[m
[31m-  const parsed = videoUrl ? parseVideoUrl(videoUrl) : null;[m
[31m-[m
[31m-  // Apply initial state (e.g. when joining a room mid-playback)[m
[31m-  useEffect(() => {[m
[31m-    const el = videoRef.current;[m
[31m-    if (!el || useIframeFallback) return;[m
[31m-    const applyInitial = () => {[m
[31m-      suppressEmit.current = true;[m
[31m-      el.currentTime = initialTime || 0;[m
[31m-      if (initialPlaying) el.play().catch(() => {});[m
[31m-      suppressEmit.current = false;[m
[31m-    };[m
[31m-    if (el.readyState >= 1) applyInitial();[m
[31m-    else el.addEventListener('loadedmetadata', applyInitial, { once: true });[m
[31m-  }, [videoUrl]); // eslint-disable-line react-hooks/exhaustive-deps[m
[31m-[m
[31m-  // Listen for remote playback events[m
[31m-  useEffect(() => {[m
[31m-    const handlePlaybackEvent = ({ type, currentTime }) => {[m
[31m-      console.log(`[client] received playback-event: ${type} @ ${currentTime?.toFixed?.(1)}s`);[m
[31m-      const el = videoRef.current;[m
[31m-      if (!el) {[m
[31m-        console.warn('[client] no video element to apply playback-event to');[m
[31m-        return;[m
[31m-      }[m
[31m-      suppressEmit.current = true;[m
[31m-[m
[31m-      if (Math.abs(el.currentTime - currentTime) > SYNC_TOLERANCE) {[m
[31m-        el.currentTime = currentTime;[m
[31m-      }[m
[31m-      if (type === 'play') el.play().catch(() => {});[m
[31m-      if (type === 'pause') el.pause();[m
[31m-      if (type === 'seek') el.currentTime = currentTime;[m
[31m-[m
[31m-      setTimeout(() => (suppressEmit.current = false), 150);[m
[31m-    };[m
[31m-[m
[31m-    const handleRoomState = (state) => {[m
[31m-      const el = videoRef.current;[m
[31m-      if (!el || state.videoUrl == null) return;[m
[31m-      suppressEmit.current = true;[m
[31m-      el.currentTime = state.currentTime;[m
[31m-      if (state.playing) el.play().catch(() => {});[m
[31m-      else el.pause();[m
[31m-      setTimeout(() => (suppressEmit.current = false), 150);[m
[31m-    };[m
[31m-[m
[31m-    socket.on('playback-event', handlePlaybackEvent);[m
[31m-    socket.on('room-state', handleRoomState);[m
[31m-    return () => {[m
[31m-      socket.off('playback-event', handlePlaybackEvent);[m
[31m-      socket.off('room-state', handleRoomState);[m
[31m-    };[m
[31m-  }, []);[m
[31m-[m
[31m-  // Periodic resync request (in case of drift/late join)[m
[31m-  useEffect(() => {[m
[31m-    const interval = setInterval(() => {[m
[31m-      socket.emit('request-sync', { roomId });[m
[31m-    }, RESYNC_INTERVAL);[m
[31m-    return () => clearInterval(interval);[m
[31m-  }, [roomId]);[m
[31m-[m
[31m-  const emitPlayback = useCallback([m
[31m-    (type) => {[m
[31m-      const el = videoRef.current;[m
[31m-      if (!el || suppressEmit.current) return;[m
[31m-      console.log(`[client] emitting playback-event: ${type} @ ${el.currentTime?.toFixed?.(1)}s`);[m
[31m-      socket.emit('playback-event', {[m
[31m-        roomId,[m
[31m-        type,[m
[31m-        currentTime: el.currentTime,[m
[31m-      });[m
[31m-    },[m
[31m-    [roomId][m
[31m-  );[m
[31m-[m
[31m-  const handleFirstInteraction = () => {[m
[31m-    ensureGraph();[m
[31m-  };[m
[31m-[m
[31m-  if (!videoUrl) {[m
[31m-    return ([m
[31m-      <div className="video-empty">[m
[31m-        <p>No video loaded yet. Paste a YouTube link, Google Drive link, or a direct video URL below.</p>[m
[31m-      </div>[m
[31m-    );[m
[31m-  }[m
[31m-[m
[31m-  if (parsed?.type === 'youtube') {[m
[31m-    return ([m
[31m-      <YouTubePlayer[m
[31m-        roomId={roomId}[m
[31m-        videoId={parsed.videoId}[m
[31m-        initialTime={initialTime}[m
[31m-        initialPlaying={initialPlaying}[m
[31m-      />[m
[31m-    );[m
[31m-  }[m
[31m-[m
[31m-  if (useIframeFallback && parsed?.type === 'drive') {[m
[31m-    return ([m
[31m-      <div className="video-wrap">[m
[31m-        <iframe[m
[31m-          src={parsed.previewUrl}[m
[31m-          allow="autoplay"[m
[31m-          allowFullScreen[m
[31m-          className="video-iframe"[m
[31m-          title="Drive video"[m
[31m-        />[m
[31m-        <p className="video-note">[m
[31m-          Playing via Google Drive preview (direct playback wasn't available for this file —[m
[31m-          make sure it's shared as "Anyone with the link can view"). Volume boost and tight sync[m
[31m-          aren't available in this fallback mode.[m
[31m-        </p>[m
[31m-      </div>[m
[31m-    );[m
[31m-  }[m
[31m-[m
[31m-  return ([m
[31m-    <div className="video-wrap" onClickCapture={handleFirstInteraction}>[m
[31m-      <video[m
[31m-        ref={videoRef}[m
[31m-        src={parsed.playableUrl}[m
[31m-        controls[m
[31m-        crossOrigin="anonymous"[m
[31m-        className="video-el"[m
[31m-        onPlay={() => emitPlayback('play')}[m
[31m-        onPause={() => emitPlayback('pause')}[m
[31m-        onSeeked={() => emitPlayback('seek')}[m
[31m-        onError={() => {[m
[31m-          if (parsed.type === 'drive') {[m
[31m-            setUseIframeFallback(true);[m
[31m-          } else {[m
[31m-            setLoadError([m
[31m-              'Could not load this video directly. Check the URL is a direct, publicly accessible video file.'[m
[31m-            );[m
[31m-          }[m
[31m-        }}[m
[31m-      />[m
[31m-      {loadError && <p className="video-error">{loadError}</p>}[m
[31m-[m
[31m-      <div className="volume-boost-panel">[m
[31m-        <label>[m
[31m-          Volume: {muted ? 'Muted' : `${gainPercent}%`}[m
[31m-          <input[m
[31m-            type="range"[m
[31m-            min="0"[m
[31m-            max={maxGain}[m
[31m-            step="0.05"[m
[31m-            value={muted ? 0 : gain}[m
[31m-            onChange={(e) => setGain(parseFloat(e.target.value))}[m
[31m-          />[m
[31m-        </label>[m
[31m-        <button onClick={toggleMute}>{muted ? 'Unmute' : 'Mute'}</button>[m
[31m-        <span className="boost-hint">Drag past 100% to boost above normal max volume</span>[m
[31m-      </div>[m
[31m-    </div>[m
[31m-  );[m
[31m-}[m
[1mdiff --git a/client/src/components/YouTubePlayer.jsx b/client/src/components/YouTubePlayer.jsx[m
[1mdeleted file mode 100644[m
[1mindex 3b83b73..0000000[m
[1m--- a/client/src/components/YouTubePlayer.jsx[m
[1m+++ /dev/null[m
[36m@@ -1,140 +0,0 @@[m
[31m-import { useEffect, useRef, useState } from 'react';[m
[31m-import { loadYouTubeApi } from '../lib/youtubeApi';[m
[31m-import { socket } from '../lib/socket';[m
[31m-[m
[31m-const SYNC_TOLERANCE = 1.5; // seconds — YouTube's seek events are less exact than <video>[m
[31m-const RESYNC_INTERVAL = 5000;[m
[31m-[m
[31m-// YT.PlayerState: -1 unstarted, 0 ended, 1 playing, 2 paused, 3 buffering, 5 cued[m
[31m-[m
[31m-export default function YouTubePlayer({ roomId, videoId, initialTime, initialPlaying }) {[m
[31m-  const containerRef = useRef(null);[m
[31m-  const playerRef = useRef(null);[m
[31m-  const [ready, setReady] = useState(false);[m
[31m-  const suppressEmit = useRef(false);[m
[31m-  const lastEmittedType = useRef(null);[m
[31m-[m
[31m-  // Create the player once per videoId[m
[31m-  useEffect(() => {[m
[31m-    let cancelled = false;[m
[31m-    setReady(false);[m
[31m-[m
[31m-    loadYouTubeApi().then((YT) => {[m
[31m-      if (cancelled || !containerRef.current) return;[m
[31m-[m
[31m-      if (playerRef.current) {[m
[31m-        playerRef.current.destroy();[m
[31m-        playerRef.current = null;[m
[31m-      }[m
[31m-[m
[31m-      playerRef.current = new YT.Player(containerRef.current, {[m
[31m-        videoId,[m
[31m-        playerVars: {[m
[31m-          autoplay: 0,[m
[31m-          playsinline: 1,[m
[31m-          rel: 0,[m
[31m-          modestbranding: 1,[m
[31m-        },[m
[31m-        events: {[m
[31m-          onReady: () => {[m
[31m-            if (cancelled) return;[m
[31m-            suppressEmit.current = true;[m
[31m-            if (initialTime) playerRef.current.seekTo(initialTime, true);[m
[31m-            if (initialPlaying) playerRef.current.playVideo();[m
[31m-            setTimeout(() => (suppressEmit.current = false), 300);[m
[31m-            setReady(true);[m
[31m-          },[m
[31m-          onStateChange: (event) => {[m
[31m-            if (suppressEmit.current) return;[m
[31m-            const player = playerRef.current;[m
[31m-            if (!player) return;[m
[31m-[m
[31m-            if (event.data === 1) {[m
[31m-              // playing[m
[31m-              if (lastEmittedType.current !== 'play') {[m
[31m-                socket.emit('playback-event', {[m
[31m-                  roomId,[m
[31m-                  type: 'play',[m
[31m-                  currentTime: player.getCurrentTime(),[m
[31m-                });[m
[31m-                lastEmittedType.current = 'play';[m
[31m-              }[m
[31m-            } else if (event.data === 2) {[m
[31m-              // paused[m
[31m-              socket.emit('playback-event', {[m
[31m-                roomId,[m
[31m-                type: 'pause',[m
[31m-                currentTime: player.getCurrentTime(),[m
[31m-              });[m
[31m-              lastEmittedType.current = 'pause';[m
[31m-            }[m
[31m-          },[m
[31m-        },[m
[31m-      });[m
[31m-    });[m
[31m-[m
[31m-    return () => {[m
[31m-      cancelled = true;[m
[31m-      if (playerRef.current) {[m
[31m-        playerRef.current.destroy();[m
[31m-        playerRef.current = null;[m
[31m-      }[m
[31m-    };[m
[31m-  }, [videoId]); // eslint-disable-line react-hooks/exhaustive-deps[m
[31m-[m
[31m-  // Listen for remote playback + room-state events[m
[31m-  useEffect(() => {[m
[31m-    const applyRemote = (type, currentTime) => {[m
[31m-      const player = playerRef.current;[m
[31m-      if (!player || typeof player.getCurrentTime !== 'function') return;[m
[31m-[m
[31m-      suppressEmit.current = true;[m
[31m-      const drift = Math.abs(player.getCurrentTime() - currentTime);[m
[31m-      if (drift > SYNC_TOLERANCE) {[m
[31m-        player.seekTo(currentTime, true);[m
[31m-      }[m
[31m-      if (type === 'play') player.playVideo();[m
[31m-      if (type === 'pause') player.pauseVideo();[m
[31m-      if (type === 'seek') player.seekTo(currentTime, true);[m
[31m-[m
[31m-      setTimeout(() => (suppressEmit.current = false), 400);[m
[31m-    };[m
[31m-[m
[31m-    const handlePlaybackEvent = ({ type, currentTime }) => {[m
[31m-      console.log(`[client] YouTube received playback-event: ${type} @ ${currentTime?.toFixed?.(1)}s`);[m
[31m-      applyRemote(type, currentTime);[m
[31m-    };[m
[31m-[m
[31m-    const handleRoomState = (state) => {[m
[31m-      if (state.videoType !== 'youtube') return;[m
[31m-      applyRemote(state.playing ? 'play' : 'pause', state.currentTime);[m
[31m-    };[m
[31m-[m
[31m-    socket.on('playback-event', handlePlaybackEvent);[m
[31m-    socket.on('room-state', handleRoomState);[m
[31m-    return () => {[m
[31m-      socket.off('playback-event', handlePlaybackEvent);[m
[31m-      socket.off('room-state', handleRoomState);[m
[31m-    };[m
[31m-  }, []);[m
[31m-[m
[31m-  // Periodic resync request[m
[31m-  useEffect(() => {[m
[31m-    const interval = setInterval(() => {[m
[31m-      socket.emit('request-sync', { roomId });[m
[31m-    }, RESYNC_INTERVAL);[m
[31m-    return () => clearInterval(interval);[m
[31m-  }, [roomId]);[m
[31m-[m
[31m-  return ([m
[31m-    <div className="video-wrap youtube-wrap">[m
[31m-      <div ref={containerRef} className="youtube-iframe-target" />[m
[31m-      {!ready && <p className="video-note">Loading YouTube player...</p>}[m
[31m-      <p className="video-note">[m
[31m-        YouTube playback goes through YouTube's own player — volume boost above 100% isn't[m
[31m-        available for YouTube videos (Google doesn't expose raw audio to embedded players).[m
[31m-        Play/pause/seek stay in sync with everyone in the room.[m
[31m-      </p>[m
[31m-    </div>[m
[31m-  );[m
[31m-}[m
[1mdiff --git a/server/index.js b/server/index.js[m
[1mindex 93b07c4..4ee6a96 100644[m
[1m--- a/server/index.js[m
[1m+++ b/server/index.js[m
[36m@@ -125,6 +125,7 @@[m [mio.on('connection', (socket) => {[m
       type,[m
       currentTime,[m
       from: socket.id,[m
[32m+[m[32m      serverTime: Date.now(),[m
     });[m
   });[m
 [m
